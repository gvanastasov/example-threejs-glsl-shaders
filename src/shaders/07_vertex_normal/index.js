import vert from './vertex_normal.vert';
import frag from './vertex_normal.frag';

export default {
    name: 'Vertex Normal',
    group: 'Unlit',
    vert,
    frag,
    props: [],
    lights: true,
}